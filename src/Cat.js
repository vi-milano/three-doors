import React, { Component } from 'react'

export default class Cat extends Component {
    render() {
        return (
            <div>
             {alert('Meow!')}
            </div>
        )
    }
}
