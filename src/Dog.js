import React, { Component } from 'react'

export default class Dog extends Component {
    render() {
        return (
            <div>
                {alert('Woof!')}
            </div>
        )
    }
}
