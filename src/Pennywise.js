import React, { Component } from 'react'

export default class Pennywise extends Component {
    render() {
        return (
            <div>
                {alert('Woof!')}
            </div>
        )
    }
}
